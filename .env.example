# ===================================
# API Configuration
# ===================================

# Base URL for internal APIs (optional)
# Leave empty if using full URLs in api-config.ts
# Example: http://localhost:3000 or https://api.yourapp.com
API_BASE_URL=

# Access token for authenticated APIs (optional)
# Will be sent as "Authorization: Bearer <token>"
ACCESS_TOKEN=

# Custom headers in JSON format (optional)
# Example: {"X-Api-Key": "your-key", "X-Custom-Header": "value"}
CUSTOM_HEADERS=
